{
  "name": "dr-broto",
  "version": "1.0.0",
  "description": "Plant analysis application",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix front\""
  },
  "dependencies": {
    "@google/genai": "^0.1.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}